name: Build an images

on: 
 pull_request:
   branches:
     - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps: 
    -  name: checkout code
       uses: actions/checkout@v3

    - name: setup of docker Build
      uses: docker/setup-buildx-action@v2
      
    - name: Build Docker image
      run: |
        docker build -t my-app-image

     
